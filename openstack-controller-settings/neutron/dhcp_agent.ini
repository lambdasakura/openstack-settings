[DEFAULT]

interface_driver = neutron.agent.linux.interface.OVSInterfaceDriver
resync_interval = 30
dhcp_driver = neutron.agent.linux.dhcp.Dnsmasq
enable_isolated_metadata = True
force_metadata = True
enable_metadata_network = True
dnsmasq_dns_servers = <dnsサーバのアドレス>

[agent]

[ovs]

